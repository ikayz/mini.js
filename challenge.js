var myNumber = ?; //Add a valid value here

// For 24 and 42
if ((myNumber >= ?) &? (myNumber ?= 42)) {
  alert(?);
} else {
  ? //Add your response
}

// For 50 and 100 start below this line on 11


// Create separate if statement like the one above for 50 and 100
// Completing this mini challenge will teach you about the If statement
